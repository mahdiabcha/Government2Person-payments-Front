@import '@angular/material/prebuilt-themes/indigo-pink.css';
@tailwind base;
@tailwind components;
@tailwind utilities;

/* Small helpers */
.app-container { min-height: 100vh; }
/* ===========================
   Global Design Tokens
   =========================== */
:root {
  --bg: #0f1524;           /* page background (dark navy) */
  --surface: #101827;      /* cards/containers */
  --surface-2: #0b1220;    /* deeper surface */
  --text: #e5e7eb;         /* primary text */
  --muted: #94a3b8;        /* secondary text */
  --border: #1f2937;       /* hairline borders */
  --primary: #4f46e5;      /* brand (indigo) */
  --primary-600: #4338ca;
  --accent: #06b6d4;       /* cyan accent */
  --success: #10b981;
  --warning: #f59e0b;
  --danger: #ef4444;

  --radius: 14px;
  --radius-sm: 10px;

  --shadow-1: 0 6px 18px rgba(0, 0, 0, .25);
  --shadow-2: 0 12px 30px rgba(2, 6, 23, .45);

  --pad: clamp(16px, 1.6vw, 24px);
}

/* (Optional) light mode token swap */
@media (prefers-color-scheme: light) {
  :root {
    --bg: #f8fafc;
    --surface: #ffffff;
    --surface-2: #f1f5f9;
    --text: #0f172a;
    --muted: #475569;
    --border: #e2e8f0;
    --shadow-1: 0 6px 18px rgba(2, 6, 23, .08);
    --shadow-2: 0 12px 30px rgba(2, 6, 23, .10);
  }
}

/* ===========================
   Base Reset & Typography
   =========================== */
*,
*::before,
*::after { box-sizing: border-box; }

html, body {
  height: 100%;
  background: var(--bg);
  color: var(--text);
  font-family: Inter, ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, 'Helvetica Neue', Arial, "Apple Color Emoji","Segoe UI Emoji";
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

a { color: inherit; text-decoration: none; }
button { font: inherit; color: inherit; }

.container {
  max-width: 1200px;
  margin-inline: auto;
  padding-inline: var(--pad);
}

/* Utilities */
.muted { color: var(--muted); }
.hidden { display: none !important; }

/* ===========================
   App Header + Sidebar polish
   (works for both Admin & Citizen)
   =========================== */
.app-shell {
  display: grid;
  grid-template-columns: 260px 1fr;
  min-height: 100dvh;
}

.app-sidebar {
  background: linear-gradient(180deg, #0b1220, #0f1524);
  border-right: 1px solid var(--border);
  padding: 18px 12px;
}

.app-sidebar .brand {
  display: flex; align-items: center; gap: 10px;
  font-weight: 700; letter-spacing: .3px;
  margin: 6px 8px 16px;
}
.app-sidebar .brand .logo {
  width: 34px; height: 34px; border-radius: 10px;
  display: grid; place-items: center; background: var(--primary);
  box-shadow: var(--shadow-1);
}

.app-nav {
  display: grid; gap: 4px; margin-top: 10px;
}
.app-nav a {
  display: flex; align-items: center; gap: 10px;
  padding: 10px 12px; border-radius: 12px; color: var(--muted);
}
.app-nav a:hover { background: var(--surface-2); color: var(--text); }
.app-nav a.active {
  background: color-mix(in lch, var(--primary) 18%, transparent);
  color: #fff;
}

.app-content {
  padding: 22px clamp(16px, 2vw, 28px);
}

.topbar {
  display: flex; align-items: center; justify-content: space-between;
  gap: 16px; margin-bottom: 16px;
}
.topbar .title {
  font-size: clamp(20px, 2.4vw, 28px);
  font-weight: 700; letter-spacing: .2px;
}
.topbar .subtitle { color: var(--muted); font-weight: 500; }

/* Card */
.card {
  background: white;
  border: 0.5px solid var(--border);
  border-radius: var(--radius);
  box-shadow: var(--shadow-3);
}

/* Nice focus ring for a11y */
:focus-visible {
  outline: 2px solid color-mix(in lch, var(--accent) 70%, white);
  outline-offset: 2px;
  border-radius: 8px;
}

/* Pill buttons / chips */
.chips { display: flex; flex-wrap: wrap; gap: 8px; }
.chip {
  padding: 6px 12px; border-radius: 999px; border: 1px solid var(--border);
  color: var(--muted); background: var(--surface-2); cursor: pointer;
}
.chip:hover { color: var(--text); border-color: color-mix(in lch, var(--text) 35%, var(--border)); }
.chip.active {
  background: color-mix(in lch, var(--primary) 18%, transparent);
  border-color: color-mix(in lch, var(--primary) 40%, var(--border));
  color: #fff;
}

/* Icon button */
.icon-btn {
  display: inline-flex; align-items: center; justify-content: center;
  gap: 8px; padding: 8px 12px; border-radius: 10px;
  background: var(--surface-2); border: 1px solid var(--border); cursor: pointer;
}
.icon-btn:hover { background: color-mix(in lch, var(--surface-2) 70%, var(--surface)); }

/* Pagination */
.pager {
  display: flex; align-items: center; gap: 8px; justify-content: flex-end;
}
.pager button {
  padding: 8px 12px; border-radius: 10px; border: 1px solid var(--border); background: var(--surface-2);
}
.pager .info { color: var(--muted); }
